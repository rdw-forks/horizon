name: CMake

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  # Customize the CMake build type here (Release, Debug, RelWithDebInfo, etc.)
  BUILD_TYPE: Release
  MYSQL_PASSWORD: horizon
  MYSQL_USERNAME: horizon
  MYSQL_DATABASE: horizon

jobs:
  cluster-dependencies:
    # The CMake configure and build commands are platform agnostic and should work equally well on Windows or Mac.
    # You can convert this to a matrix build if you need cross-platform coverage.
    # See: https://docs.github.com/en/free-pro-team@latest/actions/learn-github-actions/managing-complex-workflows#using-a-build-matrix
    runs-on: self-hosted

    steps:
    - name: VCPKG Install
      run: vcpkg install vcpkg-cmake:x64-windows readline:x64-windows lua:x64-windows zlib:x64-windows sol2:x64-windows libmysql:x64-windows mysql-connector-cpp:x64-windows
    
    - name: VCPKG Install Boost
      run: vcpkg install boost-log:x64-windows boost-filesystem:x64-windows boost-system:x64-windows boost-test:x64-windows boost-locale:x64-windows boost-crc:x64-windows boost-multi-array:x64-windows

    - name : VCPKG Integrate
      run: vcpkg integrate install

  testing:
    runs-on: self-hosted
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Build
      run: . ./horizon.sh build-tests
    - name: Run
      run: . ./horizon.sh run-tests "build\bin\Release"
  build-and-install:
    runs-on: self-hosted
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Configure Step & Build
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      run: . ./horizon.sh build ${{github.workspace}}\build -DCMAKE_TOOLCHAIN_FILE="C:/vcpkg/scripts/buildsystems/vcpkg.cmake" -DVCPKG_TARGET_TRIPLET=x64-windows -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}} -DCMAKE_INSTALL_PREFIX="${{ github.workspace }}\build"  
    - name: Install
      run: . ./horizon.sh install "build/bin/Release"
    - name: Install Database
      run: . ./horizon.sh createdb horizon ${{env.MYSQL_USERNAME}} -p${{env.MYSQL_PASSWORD}};
           . ./horizon.sh importdb horizon ${{env.MYSQL_USERNAME}} -p${{env.MYSQL_PASSWORD}};
           . ./horizon.sh adduser horizon horizon horizon ${{env.MYSQL_USERNAME}} ${{env.MYSQL_PASSWORD}};
    - name: Run
      run: . ./horizon.sh auth build\bin\Release --with-config="build\bin\Release\config\auth-server.lua" --test-run
           . ./horizon.sh char build\bin\Release --with-config="build\bin\Release\config\char-server.lua" --test-run
           . ./horizon.sh zone build\bin\Release --with-config="build\bin\Release\config\zone-server.lua" --test-run
      
  cleanup:
    runs-on: self-hosted
    steps:
    - name: Cleanup
      run: . ./horizon.sh dropdb horizon ${{env.MYSQL_USERNAME}} ${{env.MYSQL_PASSWORD}};
